[package]
name = "yl"
version = "0.1.0"
edition = "2024"
authors = ["Scott A. Idler"]
build = "build.rs"
description = "A CLI application generated by rust-scaffold"

[dependencies]
clap = { version = "4.5.47", features = ["derive"] }
eyre = "0.6.12"
serde = { version = "1.0.219", features = ["derive"] }
serde_yaml = "0.9.34"
serde_json = "1.0.128"
walkdir = "2.4.0"
regex = "1.10.0"
dirs = "6.0.0"
# Phase 1 dependencies - will add more in later phases

[dev-dependencies]
tempfile = "3.8.0"

[build-dependencies]
# For build.rs if needed
